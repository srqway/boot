security:
  oauth2:
    resource:
      id: resource_id
spring:
  datasource:
    password: ''
    url: jdbc:h2:file:/tmp/h2
    username: sa
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
logging:
  level:
    idv.hsiehpinghan: INFO
    root: INFO