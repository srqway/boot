server:
  port: '8080'
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hsiehpinghan/boot/
          search-paths:
          - spring-cloud-config-server-boot/src/test/resources/spring-cloud-config-client-boot
